export type Event =
  | {
      type: "click";
      event: MouseEvent;
    }
  | {
      type: "focus";
      event: FocusEvent;
    }
  | {
      type: "keydown";
      event: KeyboardEvent;
    };

// exclude the keydown event
type NonKeyDownEvents = Exclude<Event, {type: "keydown"}>;